const { ROLE_DISPLAY_NAME } = require("../../../utils/roleLabels");
const { getByPhone, saveProfile, mergeFromStorageIntoApp } = require("../../../utils/userProfileStore");
const { getSchoolsByKind, getSchoolName } = require("../../../utils/schoolsMock");
const { matchGradeToPicker } = require("../../../utils/gradeOptions");
const { matchClassTimeToForm, parseTimeSelection, serializeTimeSelection, isValidTimeSelection } = require("../../../utils/classTimeOptions");
const { checkOnboardingOrRedirect } = require("../../../utils/onboardingGuard");
const { studentApi, teacherApi, authApi, adminApi } = require("../../../utils/api");
const { freeTimeMapsToSerializedString, weekIdsSlotIdsToFreeTimeMaps } = require("../../../utils/dtoMappers");

const roleFieldMap = { teacher: ["name", "workNo", "schoolId", "grade", "subjects", "personality", "availableTime"], student: ["name", "studentNo", "schoolId", "grade", "studentAvailableTime", "subjects", "guardianContact", "learningGoal"], admin_level_2: ["name", "school", "region", "managedStudents"], admin_level_1: ["name", "organization", "permissionNote"] };
const roleMetaMap = { teacher: { title: "资料与账号" }, student: { title: "资料与账号" }, admin_level_2: { title: "资料与账号" }, admin_level_1: { title: "资料与账号" } };
const sectionMetaMap = { basic: { title: "基本信息", desc: "" }, ability: { title: "教学信息", desc: "" }, learning: { title: "学习信息", desc: "" }, region: { title: "区域", desc: "" }, platform: { title: "平台", desc: "" } };
const fieldMetaMap = { name: { label: "姓名", placeholder: "请输入姓名", type: "input", section: "basic", hint: "" }, studentNo: { label: "学号", placeholder: "请填写学号", type: "input", section: "basic", hint: "" }, workNo: { label: "学号/学工号", placeholder: "请填写学号或学工号", type: "input", section: "basic", hint: "" }, school: { label: "学校", placeholder: "请输入学校", type: "input", section: "basic", hint: "" }, studentAvailableTime: { label: "可在线/希望上课时间", placeholder: "", type: "input", section: "learning", hint: "" }, grade: { label: "年级", placeholder: "请输入年级", type: "input", section: "basic", hint: "" }, subjects: { label: "科目", placeholder: "如：数学、英语", type: "input", section: "ability", hint: "" }, personality: { label: "性格描述", placeholder: "选填", type: "textarea", section: "ability", hint: "" }, availableTime: { label: "可授课时间", placeholder: "", type: "input", section: "ability", hint: "" }, guardianContact: { label: "监护人联系方式", placeholder: "选填", type: "input", section: "learning", hint: "" }, learningGoal: { label: "学习目标", placeholder: "选填", type: "textarea", section: "learning", hint: "" }, region: { label: "管辖区域", placeholder: "选填", type: "input", section: "region", hint: "" }, managedStudents: { label: "代管学员", placeholder: "选填", type: "textarea", section: "region", hint: "" }, organization: { label: "所属机构", placeholder: "选填", type: "input", section: "platform", hint: "" }, permissionNote: { label: "备注", placeholder: "选填", type: "textarea", section: "platform", hint: "" } };
const roleSectionOrderMap = { teacher: ["basic", "ability"], student: ["basic", "learning"], admin_level_2: ["basic", "region"], admin_level_1: ["basic", "platform"] };
function buildSections(role, form) { let fieldKeys = roleFieldMap[role] || roleFieldMap.student; if (role === "admin_level_2" && form.l2Scope === "recipient_side") fieldKeys = fieldKeys.filter((k) => k !== "managedStudents"); const visibleFields = fieldKeys; const grouped = {}; visibleFields.forEach((key) => { if (key === "schoolId" && (role === "student" || role === "teacher")) { const kind = role === "student" ? "recipient" : "support"; const range = getSchoolsByKind(kind); const sid = (form.schoolId && String(form.schoolId)) || ""; const sectionKey = "basic"; if (!grouped[sectionKey]) grouped[sectionKey] = { key: sectionKey, title: sectionMetaMap[sectionKey].title, desc: sectionMetaMap[sectionKey].desc, fields: [] }; const byIndex = sid ? range.findIndex((s) => s.id === sid) : -1; const lineText = !sid ? "" : byIndex >= 0 ? (range[byIndex] && range[byIndex].name) || "" : getSchoolName(sid) || ""; const pickerValue = byIndex >= 0 ? byIndex : 0; grouped[sectionKey].fields.push({ key: "schoolId", label: "学校", type: "picker_school", range, lineText, hasValue: !!sid, pickerValue, hint: "" }); return; } if (key === "grade" && role === "student") { const mg = matchGradeToPicker(form.grade); const gRange = mg.list; const gix = mg.index >= 0 ? mg.index : 0; const sectionKey = "basic"; if (!grouped[sectionKey]) grouped[sectionKey] = { key: sectionKey, title: sectionMetaMap[sectionKey].title, desc: sectionMetaMap[sectionKey].desc, fields: [] }; const hasG = mg.index >= 0 && (gRange[mg.index] && gRange[mg.index].id); const lineG = hasG && gRange[mg.index] ? gRange[mg.index].name : ""; grouped[sectionKey].fields.push({ key: "grade", label: "年级", type: "picker_grade", range: gRange, lineText: lineG, hasValue: !!hasG, pickerValue: gix, hint: "" }); return; } if (key === "studentAvailableTime" && role === "student") { const tm = matchClassTimeToForm(form.studentAvailableTime); const sectionKey = "learning"; if (!grouped[sectionKey]) grouped[sectionKey] = { key: sectionKey, title: sectionMetaMap[sectionKey].title, desc: sectionMetaMap[sectionKey].desc, fields: [] }; grouped[sectionKey].fields.push({ key: "studentAvailableTime", label: "可在线/希望上课时间", type: "time_chips", tfield: "studentAvailableTime", weekChips: tm.weekChips, slotChips: tm.slotChips, hint: "" }); return; } if (key === "availableTime" && role === "teacher") { const tm = matchClassTimeToForm(form.availableTime); const sectionKey = "ability"; if (!grouped[sectionKey]) grouped[sectionKey] = { key: sectionKey, title: sectionMetaMap[sectionKey].title, desc: sectionMetaMap[sectionKey].desc, fields: [] }; grouped[sectionKey].fields.push({ key: "availableTime", label: "可授课时间", type: "time_chips", tfield: "availableTime", weekChips: tm.weekChips, slotChips: tm.slotChips, hint: "" }); return; } const meta = fieldMetaMap[key]; if (!meta) return; const sectionKey = meta.section; if (!grouped[sectionKey]) grouped[sectionKey] = { key: sectionKey, title: sectionMetaMap[sectionKey].title, desc: sectionMetaMap[sectionKey].desc, fields: [] }; grouped[sectionKey].fields.push({ key, label: meta.label, placeholder: meta.placeholder, type: meta.type, hint: meta.hint, value: form[key] != null && form[key] !== undefined ? form[key] : "" }); }); return (roleSectionOrderMap[role] || ["basic"]).map((key) => grouped[key]).filter(Boolean); }
function isFilledKey(key, form) { if (key === "schoolId") return !!((form.schoolId && String(form.schoolId).trim()) || (form.school && String(form.school).trim())); if (key === "grade") { const g = (form.grade && String(form.grade).trim()) || ""; return g.length > 0 && g !== "请选择年级"; } if (key === "studentAvailableTime") { const p = parseTimeSelection((form && form.studentAvailableTime) || ""); return isValidTimeSelection(p.weekIds, p.slotIds); } if (key === "availableTime") { const p = parseTimeSelection((form && form.availableTime) || ""); return isValidTimeSelection(p.weekIds, p.slotIds); } const v = form[key]; if (v == null) return false; return String(v).trim().length > 0; }

Page({
  data: { role: "", roleLabel: "", accAvatarUrl: "", accAvatarChar: "用", accNickname: "", accPhoneDisplay: "—", visibleFields: [], heroTitle: "", sections: [], form: { name: "", studentNo: "", l2Scope: "", workNo: "", school: "", schoolId: "", grade: "", studentAvailableTime: "", subjects: "", personality: "", availableTime: "", guardianContact: "", learningGoal: "", region: "", managedStudents: "", organization: "", permissionNote: "" } },
  onLoad() { this.syncPage(); },
  async onShow() { checkOnboardingOrRedirect("pages/common/profile/index"); mergeFromStorageIntoApp(); this.syncPage(); await this.trySyncProfileFromBackend(); },
  onEditAccount() { const app = getApp(); const r = (this.data.role || (app.globalData && app.globalData.role) || "").trim(); const u = (app.globalData && app.globalData.userInfo) || {}; if (!r || !u.phone) { wx.navigateTo({ url: `/pages/common/auth/index?role=${encodeURIComponent(r || "")}&flow=register&prefill=1&returnTo=profile` }); return; } wx.showToast({ title: "点击头像/昵称/手机直接修改", icon: "none" }); },
  async onAccChooseAvatar(e) { const url = e && e.detail && e.detail.avatarUrl ? String(e.detail.avatarUrl).trim() : ""; if (!url) { wx.showToast({ title: "头像获取失败", icon: "none" }); return; } await this._persistAccount({ avatarUrl: url }, "头像已更新"); },
  onEditNickname() { const current = (this.data.accNickname && this.data.accNickname !== "未设置昵称") ? this.data.accNickname : ""; wx.showModal({ title: "修改昵称", editable: true, placeholderText: "请输入昵称", content: current, success: async (res) => { if (!res.confirm) return; const v = (res.content || "").trim(); if (!v) { wx.showToast({ title: "昵称不能为空", icon: "none" }); return; } if (v.length > 20) { wx.showToast({ title: "昵称最多 20 个字", icon: "none" }); return; } await this._persistAccount({ nickname: v }, "昵称已更新"); } }); },
  onEditPhone() {
    wx.showToast({ title: "手机号绑定后不可修改", icon: "none" });
  },
  async _persistAccount(partial, okTitle) {
    const app = getApp();
    const u = (app.globalData && app.globalData.userInfo) || {};
    const role = (this.data.role || app.globalData.role || "").trim();
    const nextNick = partial.nickname != null ? partial.nickname : (u.nickname || "");
    const nextAvatar = partial.avatarUrl != null ? partial.avatarUrl : (u.avatarUrl || u.avatar || "");
    // 仅更新本地缓存（后端没有提供单独的资料更新接口）
    app.setLogin(role, { ...u, nickname: nextNick, avatarUrl: nextAvatar });
    this.syncPage();
    wx.showToast({ title: okTitle || "已更新", icon: "success" });
  },
  _loadFormFromStore() { const u = getApp().globalData.userInfo || {}; const p = (u.phone && getByPhone(String(u.phone))) || u; return { l2Scope: p.l2Scope != null && p.l2Scope !== "" ? String(p.l2Scope) : "", name: (p.name && String(p.name).trim()) || (p.realName && String(p.realName).trim()) || "", studentNo: p.studentNo != null && p.studentNo !== "" ? String(p.studentNo) : "", workNo: p.workNo != null && p.workNo !== "" ? String(p.workNo) : "", school: (p.school && String(p.school)) || "", schoolId: (p.schoolId && String(p.schoolId)) || "", grade: p.grade != null && p.grade !== "" ? String(p.grade) : "", studentAvailableTime: p.studentAvailableTime != null && p.studentAvailableTime !== "" ? String(p.studentAvailableTime) : "", subjects: p.subjects != null ? String(p.subjects) : "", personality: p.personality != null ? String(p.personality) : "", availableTime: p.availableTime != null ? String(p.availableTime) : "", guardianContact: p.guardianContact != null ? String(p.guardianContact) : "", learningGoal: p.learningGoal != null ? String(p.learningGoal) : "", region: p.region != null ? String(p.region) : "", managedStudents: p.managedStudents != null ? String(p.managedStudents) : "", organization: p.organization != null ? String(p.organization) : "", permissionNote: p.permissionNote != null ? String(p.permissionNote) : "" }; },
  syncPage(mergeForm) { const fromStore = this._loadFormFromStore(); const form = { ...fromStore, ...((mergeForm && typeof mergeForm === "object" && mergeForm) || {}) }; const role = getApp().globalData.role || ""; const visibleFields = roleFieldMap[role] || roleFieldMap.student; const roleMeta = roleMetaMap[role] || roleMetaMap.student; const u0 = (getApp().globalData && getApp().globalData.userInfo) || {}; const nick0 = (u0.nickname || "").trim() || "未设置昵称"; const dig = String(u0.phone || "").replace(/\D/g, "").slice(0, 11); const accPhoneDisplay = dig.length === 11 ? dig.replace(/^(\d{3})\d{4}(\d{4})$/, "$1****$2") : dig.length > 0 ? dig : "未填写手机号"; this.setData({ role, roleLabel: ROLE_DISPLAY_NAME[role] || "访客", visibleFields, heroTitle: roleMeta.title, sections: buildSections(role, form), form, accAvatarUrl: (u0.avatarUrl || "").trim(), accAvatarChar: nick0.length ? nick0.charAt(0) : "用", accNickname: nick0, accPhoneDisplay }); },
  async trySyncProfileFromBackend() { const role = this.data.role || ""; if (role !== "student" && role !== "teacher" && role !== "admin_level_1" && role !== "admin_level_2") return; try { if (role === "admin_level_1" || role === "admin_level_2") { const remote = await adminApi.myProfile(); if (!remote) return; const f = { ...this.data.form }; f.name = remote.realName || f.name || ""; f.schoolId = remote.schoolId != null ? String(remote.schoolId) : (f.schoolId || ""); f.region = remote.regionCode || f.region || ""; if (remote.permissions) { const app = getApp(); const u = (app.globalData && app.globalData.userInfo) || {}; u.permissions = Array.isArray(remote.permissions) ? remote.permissions : []; app.globalData.userInfo = u; } this.syncPage(f); return; } const remote = role === "student" ? await studentApi.getProfile() : await teacherApi.getProfile(); if (!remote) return; const f = { ...this.data.form }; f.name = remote.realName || f.name || ""; f.schoolId = remote.schoolId != null ? String(remote.schoolId) : (f.schoolId || ""); if (role === "teacher") f.school = remote.school || f.school || ""; f.grade = remote.grade || f.grade || ""; f.personality = remote.personalityDesc || f.personality || ""; if (role === "student") { const sub = Array.isArray(remote.subjectsNeeded) ? remote.subjectsNeeded.join("、") : ""; f.subjects = sub || f.subjects || ""; f.studentAvailableTime = Array.isArray(remote.freeTime) ? freeTimeMapsToSerializedString(remote.freeTime) : f.studentAvailableTime || ""; } else { const sub = Array.isArray(remote.skilledSubjects) ? remote.skilledSubjects.join("、") : ""; f.subjects = sub || f.subjects || ""; f.availableTime = Array.isArray(remote.freeTime) ? freeTimeMapsToSerializedString(remote.freeTime) : f.availableTime || ""; } this.syncPage(f); } catch (e) { if (console && console.warn) console.warn("[profile] sync backend profile failed", e); } },
  onPickSchool(e) { const ix = Number(e.detail.value); const f = { ...this.data.form }; const r = this.data.role; const range = r === "student" ? getSchoolsByKind("recipient") : getSchoolsByKind("support"); const one = range[ix]; if (one) { f.schoolId = String(one.id); f.school = getSchoolName(one.id) || one.name; } this.syncPage(f); },
  onPickGrade(e) { const ix = Number(e.detail.value); const f = { ...this.data.form }; const mg0 = matchGradeToPicker(f.grade); const g = (mg0.list || [])[ix]; f.grade = g && g.id ? g.name : ""; this.syncPage(f); },
  onToggleProfileTime(e) { const tfield = (e.currentTarget && e.currentTarget.dataset && e.currentTarget.dataset.tfield) || ""; const tkind = (e.currentTarget && e.currentTarget.dataset && e.currentTarget.dataset.tkind) || ""; const raw = (e.currentTarget && e.currentTarget.dataset && e.currentTarget.dataset.id) || ""; if (!tfield || (tkind !== "week" && tkind !== "slot")) return; const f = { ...this.data.form }; const { weekIds, slotIds } = parseTimeSelection(f[tfield] || ""); let wk = (weekIds || []).slice(); let sk = (slotIds || []).slice(); if (tkind === "week") { const n = +raw; if (n < 1 || n > 7) return; const ix = wk.indexOf(n); if (ix >= 0) wk.splice(ix, 1); else { wk.push(n); wk.sort((a, b) => a - b); } } else { const s = String(raw); if (["mor", "noon", "night"].indexOf(s) < 0) return; const j = sk.indexOf(s); if (j >= 0) sk.splice(j, 1); else sk.push(s); } f[tfield] = serializeTimeSelection(wk, sk); this.syncPage(f); },
  onInput(e) { const field = e.currentTarget.dataset.field; const value = e.detail.value; const nextForm = { ...this.data.form, [field]: value }; this.syncPage(nextForm); },
  async onSubmit() { const { form, visibleFields, role } = this.data; if (!form.name || !form.name.trim()) { wx.showToast({ title: "请填写姓名", icon: "none" }); return; } if (role === "student" && visibleFields.includes("studentNo") && !String(form.studentNo || "").trim()) { wx.showToast({ title: "请填写学号", icon: "none" }); return; } if (role === "teacher" && visibleFields.includes("workNo") && !String(form.workNo || "").trim()) { wx.showToast({ title: "请填写学号或学工号", icon: "none" }); return; } if (visibleFields.includes("schoolId") && !(form.schoolId || "").trim()) { wx.showToast({ title: "请选择学校", icon: "none" }); return; } if (visibleFields.includes("school") && !form.school.trim() && !visibleFields.includes("schoolId")) { wx.showToast({ title: "请填写学校", icon: "none" }); return; } if (visibleFields.includes("grade")) { const g = String(form.grade || "").trim(); if (!g || (role === "student" && g === "请选择年级")) { wx.showToast({ title: role === "student" ? "请选择年级" : "请填写年级", icon: "none" }); return; } } if (role === "student") { const ps = parseTimeSelection(String(form.studentAvailableTime || "")); if (!isValidTimeSelection(ps.weekIds, ps.slotIds)) { wx.showToast({ title: "请至少选一天与时段", icon: "none" }); return; } } if (role === "teacher") { const pa = parseTimeSelection(String(form.availableTime || "")); if (!isValidTimeSelection(pa.weekIds, pa.slotIds)) { wx.showToast({ title: "请至少选可授课的星期与时段", icon: "none" }); return; } } const u = getApp().globalData.userInfo || {}; if (u.phone) { const name = (form.name || "").trim(); const patch = { name, studentNo: (form.studentNo && String(form.studentNo).trim()) || "", workNo: (form.workNo && String(form.workNo).trim()) || "", school: (form.schoolId && getSchoolName(form.schoolId)) || form.school, schoolId: form.schoolId || u.schoolId, grade: form.grade, studentAvailableTime: form.studentAvailableTime, subjects: form.subjects, personality: form.personality, availableTime: form.availableTime, guardianContact: form.guardianContact, learningGoal: form.learningGoal, region: form.region, managedStudents: form.managedStudents, organization: form.organization, permissionNote: form.permissionNote }; Object.keys(patch).forEach((k) => { if (patch[k] === "" || patch[k] == null) delete patch[k]; }); saveProfile(String(u.phone), patch); } try { if (role === "student") { const t = parseTimeSelection(String(form.studentAvailableTime || "")); const freeTime = weekIdsSlotIdsToFreeTimeMaps(t.weekIds, t.slotIds); const payload = { realName: String(form.name || "").trim(), schoolId: form.schoolId ? Number(form.schoolId) : null, grade: String(form.grade || "").trim(), subjectsNeeded: String(form.subjects || "").split(/[，,\s]+/).map((x) => x.trim()).filter(Boolean), freeTime, personalityDesc: String(form.personality || "").trim() || null }; await studentApi.updateProfile(payload); await studentApi.submitProfile(); } else if (role === "teacher") { const t = parseTimeSelection(String(form.availableTime || "")); const freeTime = weekIdsSlotIdsToFreeTimeMaps(t.weekIds, t.slotIds); const payload = { realName: String(form.name || "").trim(), school: String((form.schoolId && getSchoolName(form.schoolId)) || form.school || "").trim(), grade: String(form.grade || "").trim(), freeTime, skilledSubjects: String(form.subjects || "").split(/[，,\s]+/).map((x) => x.trim()).filter(Boolean), personalSkills: "", personalityDesc: String(form.personality || "").trim() || null }; const appUser = getApp().globalData.userInfo || {}; if (appUser.hasProfile) { await teacherApi.updateProfile(payload); } else { await teacherApi.createProfile(payload); appUser.hasProfile = true; getApp().globalData.userInfo = appUser; wx.setStorageSync("userInfo", appUser); } } wx.showToast({ title: "资料已保存", icon: "success" }); } catch (err) { wx.showToast({ title: (err && err.message) || "保存失败", icon: "none" }); } }
});
