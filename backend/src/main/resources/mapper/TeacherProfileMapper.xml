<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rural.education.model.mapper.TeacherProfileMapper">

    <select id="selectRecommendations" resultType="com.rural.education.vo.TeacherVO">
        SELECT user_id AS teacherId,
               real_name AS realName,
               school,
               grade,
               skilled_subjects AS skilledSubjects,
               free_time AS freeTime
        FROM teacher_profile
        WHERE certification_status = 1  <!-- APPROVED -->
          AND continuous_match = 1      <!-- true -->
        ORDER BY total_service_duration ASC
        LIMIT 20
    </select>

    <update id="accumulateDuration">
        UPDATE teacher_profile
        SET total_service_duration = total_service_duration + #{duration}
        WHERE user_id = #{userId}
    </update>

</mapper>
