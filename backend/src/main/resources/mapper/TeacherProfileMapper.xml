<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rural.education.mapper.TeacherProfileMapper">

    <select id="selectRecommendations" resultType="map">
        SELECT user_id AS teacherId,
               real_name AS realName,
               school,
               grade,
               skilled_subjects AS skilledSubjects,
               free_time AS freeTime
        FROM teacher_profile
        WHERE certification_status = 1
          AND continuous_match = 1
        ORDER BY total_service_duration ASC
        LIMIT 20
    </select>

</mapper>
