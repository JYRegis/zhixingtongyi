<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rural.education.model.mapper.SchoolMapper">

    <select id="selectRegionDistribution" resultType="com.rural.education.dto.response.admin.RegionDistributionResponse">
        SELECT s.region_code     AS regionCode,
               s.name            AS regionName,
               COUNT(DISTINCT s.id)        AS schoolCount,
               COUNT(DISTINCT sp.user_id)  AS studentCount
        FROM school s
                 LEFT JOIN student_profile sp ON s.id = sp.school_id
        GROUP BY s.region_code, s.name
        ORDER BY studentCount DESC
    </select>

</mapper>
