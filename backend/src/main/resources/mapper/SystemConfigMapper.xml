<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rural.education.model.mapper.SystemConfigMapper">

    <select id="selectByKey" resultType="com.rural.education.model.entity.SystemConfig">
        SELECT id, config_key AS configKey, config_value AS configValue,
               description, create_time AS createTime, update_time AS updateTime
        FROM system_config
        WHERE config_key = #{configKey}
    </select>

</mapper>
